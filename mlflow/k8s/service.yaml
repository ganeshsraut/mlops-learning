apiVersion: v1
kind: Service
metadata:
  name: cnn-classifier-service
  namespace: cnn-classifier
  labels:
    app: cnn-classifier
spec:
  type: NodePort
  selector:
    app: cnn-classifier
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
